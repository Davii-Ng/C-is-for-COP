#include <stdio.h>
#include <math.h>


int main(){
    int x = 9;
    float y = 3.14;


    //x = sqrt(x);
    //x = pow(x, 3);
    // x = abs(x);
    // x = log(x);
    y = round(y);

    printf("%d\n", x);
    printf("%f", y);
    return 0;
}